SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2Autocomplete con valorb2la consulta esSELECT DISTINCT c.* 
            FROM city c 
            INNER JOIN property p ON p.code_city = c.code_city INNER JOIN property_type pt ON p.code_prop = pt.code_prop
                  INNER JOIN type t ON pt.code_type = t.code_type
                  WHERE t.code_type = '2' c.name_city LIKE '%b%'Autocomplete con valorb2la consulta esSELECT DISTINCT c.* 
            FROM city c 
            INNER JOIN property p ON p.code_city = c.code_city INNER JOIN property_type pt ON p.code_prop = pt.code_prop
                  INNER JOIN type t ON pt.code_type = t.code_type
                  WHERE t.code_type = '2' c.name_city LIKE '%b%'Autocomplete con valorbA22la consulta esSELECT DISTINCT c.* 
            FROM city c 
            INNER JOIN property p ON p.code_city = c.code_city INNER JOIN property_type pt ON p.code_prop = pt.code_prop
                  INNER JOIN type t ON pt.code_type = t.code_type
                  WHERE t.code_type = '2' INNER JOIN property_activity pa ON p.code_prop = pa.code_prop
                      INNER JOIN activity a ON pa.code_act = a.code_act
                      WHERE a.code_act = '2' c.name_city LIKE '%bA%'Autocomplete con valorvla consulta esSELECT DISTINCT c.* 
            FROM city c 
            INNER JOIN property p ON p.code_city = c.code_city
            INNER JOIN property_type pt ON p.code_prop = pt.code_prop
            INNER JOIN type t ON pt.code_type = t.code_type
            INNER JOIN property_activity pa ON p.code_prop = pa.code_prop
            INNER JOIN activity a ON pa.code_act = a.code_act WHERE c.name_city LIKE '%v%'la consulta devuelveSELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 2SELECT DISTINCT a.*
                FROM Property p
                INNER JOIN Property_Type pt ON p.code_prop = pt.code_prop
                INNER JOIN Type t ON pt.code_type = t.code_type
                INNER JOIN Property_Activity pa ON p.code_prop = pa.code_prop
                INNER JOIN Activity a ON pa.code_act = a.code_act
                WHERE t.code_type = 3